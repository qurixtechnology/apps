# Built app bundles are generated by tools/build.mjs. Pin them to LF so they
# don't churn across platforms, suppress their (huge, useless) diffs, and mark
# them as generated so GitHub collapses them in reviews.
dist/** text eol=lf -diff linguist-generated
